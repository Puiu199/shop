import UserSettings from "../components/products/UserSettings";

const Settings = () => {
  return (
    <div className="bg-gradient-to-r from-blue-100 via-blue-200 to-blue-300 h-[100vh] pt-50">
      <UserSettings />
    </div>
  );
};

export default Settings;
